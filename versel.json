{
    "version": ,
    "builds": [
        {
            "src": "dist/index.js",
            "use": "@vercel/node",
            "config": { "includeFiles": ["dist/"] }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/server/app.js"
        }
    ]
}